<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SeniorCarePlus" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.SeniorCarePlus.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SeniorCarePlus.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SeniorCarePlus.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
    <!-- Splash Screen Theme -->
    <style name="Theme.SeniorCarePlus.Splash" parent="Theme.SeniorCarePlus">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@android:color/white</item>
    </style>
</resources>